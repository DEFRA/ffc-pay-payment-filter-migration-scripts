INSERT INTO "paymentRequests"(
  "schemeId",
  "sourceSystem",
  "deliveryBody",
  "invoiceNumber",
  frn,
  sbi,
  "agreementNumber",
  "contractNumber",
  currency,
  schedule,
  "dueDate",
  value,
  received,
  "marketingYear",
  "paymentRequestNumber",
  ledger,
  "debtType",
  "recoveryDate",
  "originalSettlementDate",
  "referenceId",
  "correlationId",
  batch,
  "paymentType",
  pillar,
  "exchangeRate",
  "eventDate",
  vendor,
  trader,
  "claimDate",
  "originalInvoiceNumber",
  "invoiceCorrectionReference",
  migrated,
  "migrationId")
SELECT
  "v2TempPaymentRequests"."schemeId",
  "v2TempPaymentRequests"."sourceSystem",
  "v2TempPaymentRequests"."deliveryBody",
  "v2TempPaymentRequests"."invoiceNumber",
  "v2TempPaymentRequests".frn,
  "v2TempPaymentRequests".sbi,
  "v2TempPaymentRequests"."agreementNumber",
  "v2TempPaymentRequests"."contractNumber",
  "v2TempPaymentRequests".currency,
  "v2TempPaymentRequests".schedule,
  "v2TempPaymentRequests"."dueDate",
  "v2TempPaymentRequests".value,
  "v2TempPaymentRequests".received,
  "v2TempPaymentRequests"."marketingYear",
  "v2TempPaymentRequests"."paymentRequestNumber",
  "v2TempPaymentRequests".ledger,
  "v2TempPaymentRequests"."debtType",
  "v2TempPaymentRequests"."recoveryDate",
  "v2TempPaymentRequests"."originalSettlementDate",
  "v2TempPaymentRequests"."referenceId",
  "v2TempPaymentRequests"."correlationId",
  "v2TempPaymentRequests".batch,
  "v2TempPaymentRequests"."paymentType",
  "v2TempPaymentRequests".pillar,
  "v2TempPaymentRequests"."exchangeRate",
  "v2TempPaymentRequests"."eventDate",
  "v2TempPaymentRequests".vendor,
  "v2TempPaymentRequests".trader,
  "v2TempPaymentRequests"."claimDate",
  "v2TempPaymentRequests"."originalInvoiceNumber",
  "v2TempPaymentRequests"."invoiceCorrectionReference",
  NOW(),
  "v2TempPaymentRequests"."migrationId"
FROM "v2TempPaymentRequests"
LEFT JOIN "paymentRequests"
  ON "v2TempPaymentRequests"."migrationId" = "paymentRequests"."migrationId"
WHERE "paymentRequests"."migrationId" IS NULL
AND "v2TempPaymentRequests"."schemeId" != 7;